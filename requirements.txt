# Hybrid Reasoning System - Python Dependencies
# Core system works with Python 3.10+

# ============================================================================
# CORE RUNTIME DEPENDENCIES (minimal for running with rules/stub)
# ============================================================================

# No external dependencies required for basic operation!
# The system uses only Python standard library (json, re, urllib, dataclasses, etc.)
# Ollama is accessed via HTTP using stdlib urllib

# ============================================================================
# NEURAL NETWORK DEPENDENCIES (required for GRU training and inference)
# ============================================================================

# PyTorch - Core ML framework for GRU
# IMPORTANT: Install PyTorch FIRST with GPU support before running pip install -r requirements.txt
#
# For your CUDA 11.8 GPU, run this first:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# Or for other configurations:
#   CPU only: pip install torch --index-url https://download.pytorch.org/whl/cpu
#   CUDA 12.1: pip install torch --index-url https://download.pytorch.org/whl/cu121
#
# Then install the rest with: pip install -r requirements.txt
#
# Uncomment ONE of these based on what you installed:
torch>=2.0.0  # Generic - will use whatever torch you pre-installed
# torch>=2.0.0+cu118  # CUDA 11.8 (if installing from PyPI with this tag)

# ============================================================================
# TRAINING DEPENDENCIES (data handling, metrics, visualization)
# ============================================================================

# Data manipulation and analysis
numpy>=1.24.0,<2.0.0
pandas>=2.0.0,<3.0.0

# Metrics and evaluation
scikit-learn>=1.3.0,<2.0.0

# Visualization (for training curves, analysis)
matplotlib>=3.7.0,<4.0.0
seaborn>=0.12.0,<1.0.0

# Progress bars for training loops
tqdm>=4.65.0,<5.0.0

# Tensorboard for training monitoring (optional but recommended)
tensorboard>=2.13.0,<3.0.0

# ============================================================================
# DEVELOPMENT DEPENDENCIES (testing, linting, type checking)
# ============================================================================

# Testing framework
pytest>=7.4.0,<8.0.0
pytest-cov>=4.1.0,<5.0.0

# Code formatting
black>=23.7.0,<24.0.0

# Linting
ruff>=0.0.280,<1.0.0

# Type checking
mypy>=1.4.0,<2.0.0

# ============================================================================
# OPTIONAL DEPENDENCIES (enhanced features)
# ============================================================================

# NetworkX - for advanced graph operations on relational memory (optional)
networkx>=3.1,<4.0.0

# Rich - for better terminal output and progress tracking (optional)
rich>=13.5.0,<14.0.0

# Hydra - for advanced config management (optional)
# hydra-core>=1.3.0,<2.0.0

# Weights & Biases - for experiment tracking (optional)
wandb>=0.15.0,<1.0.0

